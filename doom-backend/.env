# ── Configuration du serveur ──
PORT=5000

# ── Configuration base de données MySQL ──
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=doom_db

# ── Clé secrète pour les tokens JWT ──
JWT_SECRET=doom_secret_key_2026
JWT_EXPIRE=7d